export class myConstant {
  static LIST_ROLES = {
    Root: 'root',
    Admin: 'admin',
    User: 'user',
  };

  static PAGINATION = {
    minPage: 1,
    minLimit: 10,
    maxLimit: Number.MAX_SAFE_INTEGER,
  };

  static RANK = {
    top: 50,
  };
}
